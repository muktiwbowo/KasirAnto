<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.digitalone.kasiranto">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.WithoutActionBar">
        <activity android:name=".akun.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".akun.SuperUserActivity" />
        <activity android:name=".kafe.KafeRenangActivity" />
        <activity android:name=".kolam.KolamIkanActivity" />
        <activity android:name=".warung.WarungActivity" />
        <activity android:name=".toko.TokoActivity"></activity>
    </application>

</manifest>